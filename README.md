# EventManager
A production-ready backend service that enables users to create events, like/unlike events, and buy/cancel tickets. Includes secure JWT authentication (HTTP-only cookies), modular controllers, atomic updates, and clean REST API design....
